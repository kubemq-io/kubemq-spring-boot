spring:
  application:
    name: kubemq-example-events-store-replay-sequence
  main:
    web-application-type: none

kubemq:
  address: ${KUBEMQ_ADDRESS:localhost:50000}
  client-id: spring-events-store-replay-sequence
