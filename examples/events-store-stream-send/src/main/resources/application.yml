spring:
  application:
    name: kubemq-example-events-store-stream-send
  main:
    web-application-type: none

kubemq:
  address: ${KUBEMQ_ADDRESS:localhost:50000}
  client-id: spring-events-store-stream-send
