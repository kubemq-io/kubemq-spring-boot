spring:
  application:
    name: kubemq-example-commands-consumer-group
  main:
    web-application-type: none

kubemq:
  address: ${KUBEMQ_ADDRESS:localhost:50000}
  client-id: spring-commands-consumer-group
