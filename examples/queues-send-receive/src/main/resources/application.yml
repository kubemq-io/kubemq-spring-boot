spring:
  application:
    name: kubemq-example-queues-send-receive
  main:
    web-application-type: none

kubemq:
  address: ${KUBEMQ_ADDRESS:localhost:50000}
  client-id: spring-queues-send-receive
