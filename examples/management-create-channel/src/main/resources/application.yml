spring:
  application:
    name: kubemq-example-management-create-channel
  main:
    web-application-type: none

kubemq:
  address: ${KUBEMQ_ADDRESS:localhost:50000}
  client-id: spring-management-create-channel
