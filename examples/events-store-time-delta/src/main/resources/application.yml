spring:
  application:
    name: kubemq-example-events-store-time-delta
  main:
    web-application-type: none

kubemq:
  address: ${KUBEMQ_ADDRESS:localhost:50000}
  client-id: spring-events-store-time-delta
